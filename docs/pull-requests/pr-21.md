---
type: pull_request
state: closed (merged)
branch: feature/19-createtree-api-rate-limit → dev
created: 2026-03-24T10:34:46Z
updated: 2026-07-16T17:32:40Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/commit-action/pull/21
comments: 14
labels: none
assignees: c-vigo
milestone: none
projects: none
merged: 2026-03-24T13:22:52Z
synced: 2026-07-17T04:59:19.683Z
---

# [PR 21](https://github.com/vig-os/commit-action/pull/21) feat: optimize createTree to reduce REST API usage (#19)

## Description

This pull request optimizes multi-file commits by using the GitHub `createTree` API's inline `content` field for text files so blobs are created server-side, instead of issuing one `createBlob` request per file. Binary files (NUL byte in the first 8 KiB) still use `createBlob` with base64 encoding. Large change sets are split into chained `createTree` calls (100 entries per request) to stay within payload limits.

This reduces REST traffic for large commits (for example, on the order of five calls plus ref/commit steps for many text files, instead of one blob per file) and is easier on GitHub App secondary rate limits. Commits remain API-created and verified the same way as before; authentication is unchanged.

## Related Issue(s)

Closes #19

## Type of Change

- [ ] Bug fix (non-breaking change which fixes an issue)
- [x] New feature (non-breaking change which adds functionality)
- [ ] Breaking change (fix or feature that would cause existing functionality to not work as expected)
- [x] Documentation update
- [ ] Refactoring (no functional changes)
- [x] Test updates

## Changes Made

- Added `isBinaryFile`, `getFileMode`, and exported `TREE_ENTRY_CHUNK_SIZE`; refactored `createTree` to use inline `content` for text, `createBlob` for binary, and chunked tree creation when there are more than 100 entries.
- Binary blob creation is sequential (not concurrent) to avoid secondary rate-limit bursts.
- Non-UTF-8 text files: `TextDecoder({ fatal: true })` with `createBlob` fallback to avoid silent data corruption.
- `isBinaryFile`: uses `bytesRead` and scans only `buf.subarray(0, bytesRead)` to avoid false positives from zero-filled buffer tails.
- Extended Jest setup and `commit.test.ts` for inline trees, binary/mixed paths, chunking, empty `filePaths` handling, and new helpers; clarified fs mock comment for binary detection.
- Documented behavior under `[Unreleased]` in `CHANGELOG.md` (Added / Changed / Fixed). Updated `README.md` (features, module exports, example `uses` pin to `@main` with note that inline optimization is unreleased until next tag).
- Regenerated `dist/` via `npm run build` and `npm run bundle`.

## Testing

- [x] Tests pass locally (`npm test`)
- [ ] Manual testing performed (describe below)

### Manual Testing Details

Not run against the live GitHub API in this environment; behavior is covered by unit tests with mocked Octokit.

## Checklist

- [x] My code follows the project's style guidelines
- [x] I have performed a self-review of my code
- [x] I have commented my code, particularly in hard-to-understand areas
- [x] I have updated the documentation accordingly (README.md, CONTRIBUTE.md, etc.)
- [x] I have updated the CHANGELOG.md in the `[Unreleased]` section
- [x] My changes generate no new warnings or errors
- [x] I have added tests that prove my fix is effective or that my feature works
- [x] New and existing unit tests pass locally with my changes
- [ ] Any dependent changes have been merged and published

## Additional Notes

- Branch was renamed from `bugfix/19-createtree-api-rate-limit` to `feature/19-createtree-api-rate-limit`; issue #19 was recategorized as an enhancement with an updated title.
- Commits are intentionally atomic: tests (red), feature, docs, dist rebuild; plus Copilot follow-ups (empty createTree test, fs mock comment, README pin to `@main`, dist).
- Payload-size-aware chunking (large inline content) is tracked in #22.


---
---

## Review Threads (7)

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 24, 2026 at 10:38 AM_

_File: [`src/commit.ts`](https://github.com/vig-os/commit-action/pull/21#discussion_r2980588834)_

```diff
@@ -59,26 +128,49 @@ export async function createTree(
   baseTreeSha: string,
   filePaths: string[]
 ): Promise<string> {
-  const treeEntries = [];
+  const isBinaryByPath = new Map<string, boolean>();
+  const binaryPaths: string[] = [];
+  for (const p of filePaths) {
+    const binary = isBinaryFile(p);
+    isBinaryByPath.set(p, binary);
+    if (binary) {
+      binaryPaths.push(p);
+    }
+  }
 
-  for (const filePath of filePaths) {
-    const { sha, mode } = await createBlob(octokit, owner, repo, filePath);
-    treeEntries.push({
+  const blobByPath = new Map<string, { sha: string; mode: "100644" | "100755" }>();
+  await Promise.all(
+    binaryPaths.map(async (filePath) => {
+      const result = await createBlob(octokit, owner, repo, filePath);
+      blobByPath.set(filePath, result);
+    })
+  );
+
+  const treeEntries: TreeBlobEntry[] = filePaths.map((filePath) => {
+    if (isBinaryByPath.get(filePath)) {
+      const { sha, mode } = blobByPath.get(filePath)!;
+      return {
+        path: filePath,
+        mode,
+        type: "blob" as const,
+        sha,
+      };
+    }
+    const content = fs.readFileSync(filePath, "utf-8");
+    const mode = getFileMode(filePath);
+    return {
       path: filePath,
-      mode: mode as "100644" | "100755",
+      mode,
       type: "blob" as const,
-      sha,
-    });
-  }
-
-  const { data: tree } = await octokit.rest.git.createTree({
-    owner,
-    repo,
-    base_tree: baseTreeSha,
-    tree: treeEntries,
+      content,
+    };
```

`createTree` reads text file contents using `fs.readFileSync(filePath, "utf-8")`. For files that contain non-UTF-8 bytes but no NUL in the first 8 KiB, Node will decode with replacement characters, which will silently change the committed content. To avoid data corruption, consider validating UTF-8 with a fatal decoder (or a round-trip check) and falling back to the `createBlob`/base64 path when decoding is not lossless.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 24, 2026 at 01:05 PM — [link](https://github.com/vig-os/commit-action/pull/21#discussion_r2981429093)

  Addressed in 687cf3e (feat): we now use `TextDecoder({ fatal: true })` for UTF-8 and fall back to `createBlob` when decoding fails.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 24, 2026 at 10:38 AM_

_File: [`src/commit.ts`](https://github.com/vig-os/commit-action/pull/21#discussion_r2980588877)_

```diff
@@ -59,26 +128,49 @@ export async function createTree(
   baseTreeSha: string,
   filePaths: string[]
 ): Promise<string> {
-  const treeEntries = [];
+  const isBinaryByPath = new Map<string, boolean>();
+  const binaryPaths: string[] = [];
+  for (const p of filePaths) {
+    const binary = isBinaryFile(p);
+    isBinaryByPath.set(p, binary);
+    if (binary) {
+      binaryPaths.push(p);
+    }
+  }
 
-  for (const filePath of filePaths) {
-    const { sha, mode } = await createBlob(octokit, owner, repo, filePath);
-    treeEntries.push({
+  const blobByPath = new Map<string, { sha: string; mode: "100644" | "100755" }>();
+  await Promise.all(
+    binaryPaths.map(async (filePath) => {
+      const result = await createBlob(octokit, owner, repo, filePath);
+      blobByPath.set(filePath, result);
+    })
+  );
```

`Promise.all(binaryPaths.map(createBlob))` will fire all binary `createBlob` API calls concurrently. If there are many binary files, this can create a burst of REST requests and undermine the PR’s goal of being friendlier to GitHub App secondary rate limits. Consider keeping blob creation sequential or using a small concurrency limit (e.g., a queue) for binary blobs.
```suggestion
  for (const filePath of binaryPaths) {
    const result = await createBlob(octokit, owner, repo, filePath);
    blobByPath.set(filePath, result);
  }
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 24, 2026 at 01:05 PM — [link](https://github.com/vig-os/commit-action/pull/21#discussion_r2981429331)

  Addressed in 687cf3e (feat): binary blob creation is now sequential via a `for` loop instead of `Promise.all`.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 24, 2026 at 10:38 AM_

_File: [`src/commit.ts (line 133 RIGHT)`](https://github.com/vig-os/commit-action/pull/21#discussion_r2980588905)_

```diff
@@ -41,16 +88,38 @@ export async function createBlob(
     encoding: "base64",
   });
 
-  // Determine file mode (100644 for regular files, 100755 for executables)
-  const stats = fs.statSync(filePath);
-  const mode: "100644" | "100755" =
-    stats.mode & parseInt("111", 8) ? "100755" : "100644";
+  const mode = getFileMode(filePath);
 
   return { sha: blob.sha, mode };
 }
 
 /**
- * Creates a Git tree with updated files via GitHub API
+ * Chains createTree calls when there are many entries (avoids huge payloads).
+ */
+async function createTreeChained(
+  octokit: ReturnType<typeof github.getOctokit>,
+  owner: string,
+  repo: string,
+  initialBaseTreeSha: string,
+  entries: TreeBlobEntry[]
+): Promise<string> {
+  let baseTreeSha = initialBaseTreeSha;
+  for (let i = 0; i < entries.length; i += TREE_ENTRY_CHUNK_SIZE) {
+    const chunk = entries.slice(i, i + TREE_ENTRY_CHUNK_SIZE);
+    const { data: tree } = await octokit.rest.git.createTree({
+      owner,
+      repo,
+      base_tree: baseTreeSha,
+      tree: chunk,
+    });
```

`createTreeChained` chunks purely by entry count (`TREE_ENTRY_CHUNK_SIZE`) but doesn’t account for total request payload size (which is dominated by inline `content`). A chunk of 100 large text files can still exceed GitHub request size limits. Consider chunking by approximate serialized size (bytes) and/or falling back to `createBlob` for large text files to guarantee requests stay under limits.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 24, 2026 at 01:05 PM — [link](https://github.com/vig-os/commit-action/pull/21#discussion_r2981429367)

  Tracked in #22: chunking by payload size (bytes) for large inline content. Current implementation chunks by entry count only; size-based limits will be explored separately.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 24, 2026 at 10:38 AM_

_File: [`src/commit.ts`](https://github.com/vig-os/commit-action/pull/21#discussion_r2980588923)_

```diff
@@ -18,6 +18,53 @@ export interface CommitResult {
   filesCommitted: number;
 }
 
+/** Max tree entries per createTree request (payload / GitHub limits). */
+export const TREE_ENTRY_CHUNK_SIZE = 100;
+
+/**
+ * Returns true if the file appears binary (null byte in first 8 KiB), matching Git's heuristic.
+ */
+export function isBinaryFile(filePath: string): boolean {
+  if (!fs.existsSync(filePath)) {
+    throw new Error(`File not found: ${filePath}`);
+  }
+  const stat = fs.statSync(filePath);
+  if (stat.size === 0) {
+    return false;
+  }
+  const toRead = Math.min(8192, stat.size);
+  const buf = Buffer.alloc(toRead);
+  const fd = fs.openSync(filePath, "r");
+  try {
+    fs.readSync(fd, buf, 0, toRead, 0);
+  } finally {
+    fs.closeSync(fd);
+  }
+  return buf.includes(0);
```

`isBinaryFile` ignores the `bytesRead` result from `fs.readSync`. If `readSync` returns fewer bytes than requested (possible on some filesystems / interruptions), the remainder of `buf` stays zero-filled from `Buffer.alloc`, which can cause false positives for binary detection. Consider capturing `bytesRead` and only scanning `buf.subarray(0, bytesRead)` (or pre-fill the buffer with a non-zero value before reading).
```suggestion
  let bytesRead = 0;
  try {
    bytesRead = fs.readSync(fd, buf, 0, toRead, 0);
  } finally {
    fs.closeSync(fd);
  }
  if (bytesRead === 0) {
    // No data read; treat as non-binary to avoid false positives from zero-filled buffer.
    return false;
  }
  return buf.subarray(0, bytesRead).includes(0);
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 24, 2026 at 01:05 PM — [link](https://github.com/vig-os/commit-action/pull/21#discussion_r2981429408)

  Addressed in 687cf3e (feat): `isBinaryFile` now uses `bytesRead` and only scans `buf.subarray(0, bytesRead)` to avoid false positives from zero-filled buffer tails.

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 24, 2026 at 01:10 PM_

_File: [`README.md`](https://github.com/vig-os/commit-action/pull/21#discussion_r2981463038)_

```diff
@@ -12,14 +12,15 @@ A modular GitHub Action that commits changes via GitHub API, creating automatica
 - **Modular design** - Can be used as a standalone action or imported as a module
 - **Type-safe** - Written in TypeScript with full type safety
 - **Well tested** - Comprehensive unit test coverage
+- **Scalable API usage** - Many text files are committed with far fewer REST calls (inline tree content), which stays friendlier to GitHub App **secondary rate limits**; binary files still use explicit blobs
 
 ## Usage
 
 ### As a GitHub Action (prebuilt bundle)
 
 ```yaml
 - name: Commit and push changes via API
-  uses: vig-os/commit-action@v0.1.4
+  uses: vig-os/commit-action@v0.1.5
```

The README now describes inline-tree behavior, but the usage example is pinned to `vig-os/commit-action@v0.1.5`. Since the changelog lists the inline `createTree` optimization under `[Unreleased]`, users copy/pasting this example won’t actually get the documented behavior. Consider pinning the example to a version that includes this change (or temporarily to `@main` until a release is cut), and keep the README + CHANGELOG in sync.
```suggestion
  uses: vig-os/commit-action@main
```

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 24, 2026 at 01:19 PM — [link](https://github.com/vig-os/commit-action/pull/21#discussion_r2981524717)

  Aligned the example to `@main` until the next release, and added a note that published tags (e.g. `@v0.1.5`) use the previous implementation. See [324ac73](https://github.com/vig-os/commit-action/commit/324ac73).

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 24, 2026 at 01:10 PM_

_File: [`src/__tests__/setup.ts`](https://github.com/vig-os/commit-action/pull/21#discussion_r2981463124)_

```diff
@@ -13,5 +13,14 @@ jest.mock("fs", () => {
     readFileSync: jest.fn(),
     statSync: jest.fn(),
     readdirSync: jest.fn(),
+    /** Used by isBinaryFile — fill read buffer so no spurious NUL bytes from Buffer.alloc */
+    openSync: jest.fn().mockReturnValue(1),
+    readSync: jest.fn((_fd, buf, offset, length) => {
+      const off = offset ?? 0;
+      const len = length ?? buf.length - off;
+      Buffer.alloc(len, 0x61).copy(buf, off);
+      return len;
+    }),
```

The comment on the mocked `readSync` says it exists to avoid false positives from `Buffer.alloc` leaving zero-filled tails. `isBinaryFile` now scans only `buf.subarray(0, bytesRead)`, so this rationale is no longer accurate. Update the comment to reflect the current reason for these mocks (providing `openSync/readSync/closeSync` for `isBinaryFile`), or remove it to avoid future confusion.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 24, 2026 at 01:19 PM — [link](https://github.com/vig-os/commit-action/pull/21#discussion_r2981525008)

  Updated the mock comment to describe why `openSync`/`readSync`/`closeSync` are mocked for `isBinaryFile` tests. See [a6fcdd3](https://github.com/vig-os/commit-action/commit/a6fcdd3).

---

### Review by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer)

_Posted on March 24, 2026 at 01:10 PM_

_File: [`src/commit.ts (line 199 RIGHT)`](https://github.com/vig-os/commit-action/pull/21#discussion_r2981463168)_

```diff
@@ -59,26 +132,64 @@ export async function createTree(
   baseTreeSha: string,
   filePaths: string[]
 ): Promise<string> {
-  const treeEntries = [];
+  const isBinaryByPath = new Map<string, boolean>();
+  const binaryPaths: string[] = [];
+  for (const p of filePaths) {
+    const binary = isBinaryFile(p);
+    isBinaryByPath.set(p, binary);
+    if (binary) {
+      binaryPaths.push(p);
+    }
+  }
+
+  const blobByPath = new Map<string, { sha: string; mode: "100644" | "100755" }>();
+  for (const filePath of binaryPaths) {
+    const result = await createBlob(octokit, owner, repo, filePath);
+    blobByPath.set(filePath, result);
+  }
+
+  const treeEntries: TreeBlobEntry[] = [];
+  const utf8Decoder = new TextDecoder("utf-8", { fatal: true });
 
   for (const filePath of filePaths) {
-    const { sha, mode } = await createBlob(octokit, owner, repo, filePath);
-    treeEntries.push({
-      path: filePath,
-      mode: mode as "100644" | "100755",
-      type: "blob" as const,
-      sha,
-    });
+    if (isBinaryByPath.get(filePath)) {
+      const { sha, mode } = blobByPath.get(filePath)!;
+      treeEntries.push({
+        path: filePath,
+        mode,
+        type: "blob" as const,
+        sha,
+      });
+      continue;
+    }
+
+    const mode = getFileMode(filePath);
+    const raw = fs.readFileSync(filePath);
+
+    try {
+      const content = utf8Decoder.decode(raw);
+      treeEntries.push({
+        path: filePath,
+        mode,
+        type: "blob" as const,
+        content,
+      });
+    } catch {
+      const result = await createBlob(octokit, owner, repo, filePath);
+      treeEntries.push({
+        path: filePath,
+        mode: result.mode,
+        type: "blob" as const,
+        sha: result.sha,
+      });
+    }
   }
 
-  const { data: tree } = await octokit.rest.git.createTree({
-    owner,
-    repo,
-    base_tree: baseTreeSha,
-    tree: treeEntries,
-  });
+  if (treeEntries.length === 0) {
+    return baseTreeSha;
+  }
 
-  return tree.sha;
+  return createTreeChained(octokit, owner, repo, baseTreeSha, treeEntries);
```

`createTree` now has an early-return path when `filePaths` is empty (`treeEntries.length === 0`). Since this is exported API surface, it would be good to add a unit test asserting it returns `baseTreeSha` and does not call `octokit.rest.git.createTree`, so the behavior is locked in.

Conversation:

- **[@c-vigo](https://github.com/c-vigo)** on March 24, 2026 at 01:19 PM — [link](https://github.com/vig-os/commit-action/pull/21#discussion_r2981525066)

  Added a unit test that `createTree(..., [])` returns `baseTreeSha` and does not call `git.createTree`. See [2fc5f27](https://github.com/vig-os/commit-action/commit/2fc5f27).


---
---

## Commits

### Commit 1: [810ba83](https://github.com/vig-os/commit-action/commit/810ba8378207116fe29faa97923587e3dd527d3e) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 10:31 AM
test: expect inline createTree and chunked trees, 283 files modified (src/__tests__/setup.ts, src/__tests__/unit/commit.test.ts)

### Commit 2: [9e6da6b](https://github.com/vig-os/commit-action/commit/9e6da6bf7bbf04afe11272a7ea23eac3d8c2cfed) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 10:31 AM
feat: reduce createTree API calls via inline content, 132 files modified (src/commit.ts)

### Commit 3: [caf4af0](https://github.com/vig-os/commit-action/commit/caf4af0738eb65a3c22d2b71c46f1a64fb68f8ed) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 10:32 AM
docs: changelog and README for createTree API efficiency, 19 files modified (CHANGELOG.md, README.md)

### Commit 4: [c64b7f4](https://github.com/vig-os/commit-action/commit/c64b7f4b0e93f2adb8a2b352e056ccd1a7deb66b) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 10:33 AM
build: regenerate dist, 469 files modified

### Commit 5: [132119b](https://github.com/vig-os/commit-action/commit/132119b4146027dc7fb0512b6b74857b586ac21a) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:04 PM
test: expect inline createTree, chunked trees, and sequential binary blobs, 57 files modified (src/__tests__/unit/commit.test.ts)

### Commit 6: [687cf3e](https://github.com/vig-os/commit-action/commit/687cf3e80e7f845e1b5c095a3a3f1cf0829c5b8d) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:04 PM
feat: sequential binary blob creation to avoid rate-limit bursts (fix #19), 57 files modified (src/commit.ts)

### Commit 7: [02473bb](https://github.com/vig-os/commit-action/commit/02473bbe0dd31180ed5480e101f55b1febe41855) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:04 PM
docs: changelog for createTree API efficiency and sequential binary blobs, 6 files modified (CHANGELOG.md)

### Commit 8: [df86f89](https://github.com/vig-os/commit-action/commit/df86f8964a53da746f05cf8cdc5d32d3f2e43d96) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:04 PM
build: regenerate dist, 153 files modified (dist/index.js, dist/src/__tests__/unit/commit.test.js, dist/src/__tests__/unit/commit.test.js.map, dist/src/commit.d.ts.map, dist/src/commit.js, dist/src/commit.js.map)

### Commit 9: [8cb3c76](https://github.com/vig-os/commit-action/commit/8cb3c76e2d05cf52b59a964394e69571d5cb382a) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:13 PM
docs(commit): document TREE_ENTRY_CHUNK_SIZE rationale, 5 files modified (src/commit.ts)

### Commit 10: [0f6e9d7](https://github.com/vig-os/commit-action/commit/0f6e9d7c812161caddf88ce6046bcfd6912b1d35) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:13 PM
refactor(commit): add isBinaryFromStat helper and CreateBlobOptions.mode, 32 files modified (src/commit.ts)

### Commit 11: [240accf](https://github.com/vig-os/commit-action/commit/240accf9753542d37709111ef2795e0393de701d) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:13 PM
refactor(commit): single stat per file, defer blobs, fresh decoder, reuse buffer on UTF-8 failure, 46 files modified (src/commit.ts)

### Commit 12: [639d12a](https://github.com/vig-os/commit-action/commit/639d12adc5857c13eb29681d1d25b1a8b796b845) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:13 PM
test(commit): add 201-file chunking test for createTree base_tree chain, 49 files modified (src/__tests__/unit/commit.test.ts)

### Commit 13: [5501722](https://github.com/vig-os/commit-action/commit/550172232b6b719c2b98d9daf19601f97ca73626) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:13 PM
build: regenerate dist, 119 files modified

### Commit 14: [2fc5f27](https://github.com/vig-os/commit-action/commit/2fc5f27d3275b0d8746e446b9dbab5cbe9c3a204) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:19 PM
test: assert createTree returns base sha when filePaths empty, 14 files modified (src/__tests__/unit/commit.test.ts)

### Commit 15: [a6fcdd3](https://github.com/vig-os/commit-action/commit/a6fcdd3b47a872205b7e36b52fb86ec398affe42) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:19 PM
chore: clarify fs mock comment for isBinaryFile tests, 2 files modified (src/__tests__/setup.ts)

### Commit 16: [324ac73](https://github.com/vig-os/commit-action/commit/324ac732c4578f15ed8b456076dbe2210b657ad3) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:19 PM
docs: pin README example to @main until inline createTree release, 4 files modified (README.md)

### Commit 17: [e22bfdd](https://github.com/vig-os/commit-action/commit/e22bfdd52524991f21834b10fa1bc74c7f1b745f) by [c-vigo](https://github.com/c-vigo) on March 24, 2026 at 01:19 PM
build: regenerate dist, 88 files modified

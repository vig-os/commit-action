---
type: pull_request
state: closed (merged)
branch: chore/105-adopt-devkit-1-3-1 → dev
created: 2026-07-17T07:45:11Z
updated: 2026-07-17T08:01:22Z
author: c-vigo
author_url: https://github.com/c-vigo
url: https://github.com/vig-os/commit-action/pull/106
comments: 0
labels: none
assignees: none
milestone: none
projects: none
merged: 2026-07-17T08:01:21Z
synced: 2026-07-18T04:44:10.975Z
---

# [PR 106](https://github.com/vig-os/commit-action/pull/106) build(devkit): adopt devkit 1.3.1

## Summary

Re-scaffold this repo from **vigOS devkit 1.2.1 → 1.3.1** (direnv mode) via the
installer (`--force --version 1.3.1`; values resolved from the existing `.vig-os`
manifest, mode/identity preserved). Pins move in lockstep:

| Pin | Value |
|-----|-------|
| `.vig-os` `DEVKIT_VERSION` | `1.3.1` |
| `flake.nix` `vigos.url` | `github:vig-os/devkit?ref=1.3.1` |
| `flake.lock` vigos rev | `5cfbac3038882cd124e7490271e26bc6490a271a` (= tag `1.3.1`) |

This is the first upgrade **across the 1.3.0 gitignore migration**, so it live-verifies devkit fix vig-os/devkit#1145.

## Verified fix signatures

**vig-os/devkit#1142 — per-language CodeQL push paths** (`.github/workflows/codeql.yml`)
```diff
     paths:
-      - '**.py'
+      - '**.ts'
+      - '**.js'
+      - '**.mjs'
+      - '**.cjs'
       - '.github/workflows/**'
```

**vig-os/devkit#1144 — release-extension caller-job token ceiling** (`.github/workflows/release.yml`, `extension` job)
```yaml
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
```

**vig-os/devkit#1145 — gitignore migration hardening**
- Consumer-owned `.gitignore.project` is **unchanged** (retains only its own `.pre-commit-config.yaml` entry) — no scaffold-committed filenames, no cross-language (Python) junk.
- Managed `.gitignore` carries no scaffold-committed names (`.envrc`, `flake.nix`, `flake.lock`, `justfile`, `justfile.project`, `.vig-os`, `.gitignore.project`); those files stay tracked.

## Other managed changes (1.2.1 → 1.3.1 template evolution)

- CI gains a `dependency-review` gate (public-repo PRs only); release/promote workflows pick up a mergeability gate and floating-tag-creation error handling; least-privilege permission tightening across release caller jobs.
- New `.github/CODEOWNERS`.
- **Consumer customization preserved:** the TypeScript-major Renovate hold (Refs #101) lived in the *managed* `renovate-default.json` and was clobbered by the re-scaffold; it has been relocated into the consumer-owned `renovate.json` (Renovate concatenates `packageRules` from the extending config, so behavior is identical) so it survives future upgrades.

## Local verification (in the 1.3.1 direnv dev-shell)

- `just precommit` (full `prek run --all-files`): **all hooks passed**.
- `npm test`: **84 passed, 3 suites, 0 failures**.

## Note

Branch is `chore/105-adopt-devkit-1-3-1` (hyphens, matching the prior `chore/79-adopt-devkit-1-2-1`) — the devkit 1.3.1 branch-naming hook rejects dots, so `chore/105-devkit-1.3.1` would fail `no-commit-to-branch`.

Closes #105


---
---

## Commits

### Commit 1: [c3ecebe](https://github.com/vig-os/commit-action/commit/c3ecebe7ccc6a4f7c59eface7d19cd4a71342b95) by [c-vigo](https://github.com/c-vigo) on July 17, 2026 at 07:44 AM
build(devkit): adopt devkit 1.3.1, 294 files modified
